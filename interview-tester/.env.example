# Test Agent Configuration
# LLM settings are read from ../interview-agent/.env

# Candidate LLM temperature (higher = more creative/diverse answers)
TEST_CANDIDATE_TEMPERATURE=0.7

# Evaluator LLM temperature (lower = more consistent evaluation)
TEST_EVALUATOR_TEMPERATURE=0.3
